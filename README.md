# SUMMON AFRICA PLAYABLE V5

Full deployable runtime: corrected V4 frontend plus the Python field server and static field bundle.

Required production routes:
- `/SUMMON_field_manifest.json`
- `/field/v1/manifest`
- `/field/v1/search`
- `/field/health`

The packaged fallback field currently contains 11 seeded records. Run the ingestion/build commands before deployment to expand it.
